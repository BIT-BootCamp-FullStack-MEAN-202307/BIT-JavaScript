let isMale = false;

if( isMale ) {
    console.log( 'H' );
}
else {
    console.log( 'F' );
}

{/* <condicion> ? <si-cumple> : <no-cumple> */}
isMale ? console.log( 'H' ) : console.log( 'F' );
console.log( isMale ? 'H' : 'F' );

const resultado = isMale ? 'H' : 'F';
console.log( resultado );


